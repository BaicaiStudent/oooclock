import 'package:flutter/material.dart'; //日历界面

void main() => runApp(new Calendar());

class Calendar extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('这里是日历'),
    );
  }
}
